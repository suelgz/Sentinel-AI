{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "streamlit run threatlensai/app.py --server.address 0.0.0.0 --server.port $PORT --server.headless true"
  }
}